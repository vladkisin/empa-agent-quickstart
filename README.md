# Agent Template (LangGraph + LangChain)

This is a generic agentic template using:
- LangGraph for stateful flow orchestration
- LangChain for LLM + tool use
- JSON-based persistence (swap with DB later)

We will fill this in step-by-step.
